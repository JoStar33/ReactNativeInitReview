import { Text, View } from "react-native";

const MainScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        height: 100,
        width: 100,
      }}
    >
      <Text>메인페이지</Text>
    </View>
  );
};

export default MainScreen;
